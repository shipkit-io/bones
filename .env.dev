# Development environment variables for Docker Compose
# These variables override the defaults in docker-compose.yml

# PostgreSQL Credentials (used by app and db services)
# You can uncomment and set these if you need different credentials for development
# POSTGRES_DB=lacy
# POSTGRES_USER=lacy_user
# POSTGRES_PASSWORD=lacy_password

# pgAdmin Credentials (used by pgadmin service)
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=admin

# Add any other development-specific environment variables here
# e.g., API keys, feature flags, etc.